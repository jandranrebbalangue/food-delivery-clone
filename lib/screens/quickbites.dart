import 'package:flutter/material.dart';
import 'package:food_delivery/widget/quickbites.dart';

class QuickBitesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Padding(padding: EdgeInsets.all(16), child: QuickBites());
  }
}
